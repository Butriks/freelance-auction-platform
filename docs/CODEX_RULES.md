# Codex Rules

- Do not rewrite the whole project unless it is necessary.
- One task equals one logical module.
- Do not add Docker.
- Do not store real secrets in the repository.
- Use `backend/.env.example`, not `backend/.env`.
- Follow the `routes/controllers/services/models` structure.
- Keep business logic in `services`, not in `routes`.

